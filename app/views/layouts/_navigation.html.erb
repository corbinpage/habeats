<nav class="navbar navbar-default navbar-fixed-top <%= (params[:controller] == "home") ? "dynamic-header" : "navbar-shrink" %>">
  <div class="container">
      <!-- Brand and toggle get grouped for better mobile display -->
      <div class="navbar-header page-scroll">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
          <span class="sr-only">Toggle navigation</span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
          <span class="icon-bar"></span>
        </button>
        <span class="fa-stack fa-2x pull-left">
          <!-- <i class="fa fa-circle fa-stack-2x text-primary"></i> -->
          <a class="consent-button" href="<% user_signed_in? ? portal_path : root_path %>">
            <i class="fa fa-user-md fa-stack-2x"></i>
          </a>
        </span>
        <a class="navbar-brand page-scroll" href="
<% user_signed_in? ? portal_path : root_path %>"> <%= ENV['company_name'] %></a>
      </div>

      <!-- Collect the nav links, forms, and other content for toggling -->
      <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
        <ul class="nav navbar-nav navbar-right">
          <%= render 'layouts/navigation_links' %>
        </ul>
      </div>
      <!-- /.navbar-collapse -->
    </div>
    <!-- /.container-fluid -->
  </nav>